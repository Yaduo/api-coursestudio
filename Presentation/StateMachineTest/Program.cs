using CourseStudio.Doamin.Models.Courses;
using System;

namespace StateMachineTest
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");

            //var course = new Course();

            //course.SubmitToAuditing();
            //course.Reject("no reason");
            //course.SubmitToAuditing();
            //course.Approve();
            //course.Release();

            //course.Update();
            //course.SubmitToAuditing();
            //course.Reject("Update reject");
            //course.SubmitToAuditing();
            //course.Approve();
            //course.Reopen();
            //course.SubmitToAuditing();
            //course.Approve();
            //course.Release();

            Console.ReadKey(false);
        }
    }
}
